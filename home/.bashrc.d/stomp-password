function stomp-password () {
  read -s -p 'input STOMP_PASSWORD (will not be echoed):' STOMP_PASSWORD && export STOMP_PASSWORD && echo "STOMP_PASSWORD exported" || echo "STOMP_PASSWORD was NOT exported"
}
